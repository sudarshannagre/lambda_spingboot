package com.sud.lambda_spingboot.util;

import org.springframework.cloud.function.adapter.aws.SpringBootRequestHandler;

public class ExportToGoogleLambdaHandler extends SpringBootRequestHandler<String, Object> {

	//run through the lambda hence this class needed
}
